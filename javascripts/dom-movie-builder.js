"use strict";

//this file will build the movie cards and push them to the dom

let $ = require('jquery');
function makeMovieList(movieList) {
	let $movieDisplay = 
	$(`<div class="uiContainer__movie-list box col s12">
		<ul`)
}